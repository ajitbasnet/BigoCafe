# Scripts

One-off or migration scripts (e.g. SQL for Supabase). Not part of the runtime app.

## Contents

- **`001_create_profiles.sql`**, **`002_profile_trigger.sql`** — Supabase schema/migrations for profiles.

## Conventions

- Run these against your Supabase project as needed (e.g. via Supabase dashboard or CLI).
- Keep filenames descriptive and ordered if there is a sequence.
