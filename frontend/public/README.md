# Public — Static Assets

Static files served at the site root: images, logos, icons, sounds.

## Conventions

- Reference in code by path from root, e.g. `/bigo-logo.png`, `/images/hero-food.jpg`.
- No build processing; keep filenames clear and lowercase/kebab-case when possible.
