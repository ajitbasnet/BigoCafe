# Hooks

Reusable React hooks (e.g. mobile detection, toast). Shared across app and components.

## Conventions

- **Naming:** `use-*.ts` or `use*.ts`; hook name in camelCase.
- **Imports:** Use `@/hooks/...` alias.
