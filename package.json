{
  "name": "bigo",
  "private": true,
  "description": "BIGO Artisan Bakery & Coffee — frontend in ./frontend",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
